#!/user/bin/env python
# -*- coding: utf-8 -*-
import pathlib

from py_mono_build.helpers.linters import (
    Bandit,
    Black,
    CheckOV,
    DocStringFormatter,
    Flake8,
    ISort,
    Mypy,
    Pydocstyle,
    Pylint,
    TFSec,
)


path = pathlib.Path(__file__).parent

NAME = "py_mono_build"

LINT = [
    Bandit(path=path),
    Black(path=path),
    DocStringFormatter(path=path),
    Flake8(path=path),
    ISort(path=path),
    Mypy(path=path),
    Pydocstyle(path=path),
    Pylint(path=path),
    TFSec(path=path),
    CheckOV(path=path),
]
TEST = []
DEPLOY = []
